{
  "scripts": {
    "build": "npx @11ty/eleventy && node generate-llms-full.js",
    "build:prod": "npx @11ty/eleventy && node generate-llms-full.js && node validate-site.js",
    "serve": "npx @11ty/eleventy --serve",
    "validate": "node validate-site.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
