{
  "scripts": {
    "build": "npx @11ty/eleventy && node generate-llms-full.js && node generate-og-images.js",
    "build:prod": "npx @11ty/eleventy && node generate-llms-full.js && node generate-og-images.js && node validate-site.js",
    "serve": "npx @11ty/eleventy --serve",
    "validate": "node validate-site.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "eleventy-plugin-nesting-toc": "1.3.0",
    "markdown-it-anchor": "9.2.0"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "satori": "^0.24.1"
  }
}
